# Step 3 — 메뉴 선택 구조 개편

> ⬅️ [목차](n8n_ml_nodes.md) | ⬅️ [Step 2 완료 후](n8n_02_피드백_로짓.md)  
> ⏱️ 예상 소요 시간: 20분

---

## 진행 현황

| 항목 | 상태 |
|------|------|
| Step 3-1: n8n B/C 경로 응답에 menus 포함 | ✅ 완료 |
| Step 3-2: React interface + handleMenuRequest 수정 | ✅ 완료 |
| Step 3-3: n8n /food/select webhook 추가 | 🔲 미완 |

---

## Step 3-3 — n8n: /webhook/food/select 추가

`먹구름_봇v2` 캔버스 빈 공간에 아래 흐름 추가:

```
메뉴 선택 입력(Webhook - /food/select)
    → 방문 기록 저장(Supabase - Create, restaurant_log)
    → 메뉴선택 현재 로짓(Supabase - Get Many, user_preference_logits)
    → 메뉴선택 로짓 계산(Code - keywords→category 매핑 + +0.1)
    → 로짓 업데이트(Supabase - Update, user_preference_logits)
    → 선택 완료 출력(Respond to Webhook)
```

---

### React에서 보내는 데이터 형태

메뉴 선택 시 React가 아래 형태로 POST:

```json
{
  "user_id": "유저UUID",
  "restaurant_id": "식당UUID",
  "keywords": ["돈까스"]
}
```

> `keywords`는 선택한 메뉴의 `menu_items.keywords` 배열을 그대로 전송.  
> 음료/뷔페 키워드만 있으면 n8n에서 자동 스킵.

---

### 메뉴 선택 입력 — Webhook

| 항목 | 값 |
|------|-----|
| **Method** | POST |
| **Path** | `food/select` |
| **Response Mode** | Using 'Respond to Webhook' Node |

### 방문 기록 저장 — Supabase Create

| Field | Value |
|-------|-------|
| `user_id` | `={{ $('메뉴 선택 입력').item.json.body.user_id }}` |
| `restaurant_id` | `={{ $('메뉴 선택 입력').item.json.body.restaurant_id }}` |
| `date` | `={{ $now.setZone('Asia/Seoul').toFormat('yyyy-MM-dd') }}` |

### 메뉴선택 현재 로짓 — Supabase Get Many

- Table: `user_preference_logits`
- Return All: ✅
- Filter: `user_id = {{ $('메뉴 선택 입력').item.json.body.user_id }}`

### 메뉴선택 로짓 계산 — Code

키워드 배열에서 카테고리를 추출해 logit +0.1 계산.  
매핑 테이블 전체는 [n8n_ml_nodes.md](n8n_ml_nodes.md#keyword--logit_category-매핑) 참고.

```javascript
const KEYWORD_CATEGORY_MAP = {
  '국밥': '한식', '해장국': '한식', '내장탕': '한식', '삼계탕': '한식',
  '닭개장': '한식', '닭볶음탕': '한식', '갈비탕': '한식',
  '김치찌개': '한식', '된장찌개': '한식', '동태찌개': '한식',
  '두부찌개': '한식', '두루치기': '한식', '낙지볶음': '한식',
  '불고기': '한식', '전골': '한식', '삼겹살': '한식', '목살': '한식',
  '갈비살': '한식', '항정살': '한식', '막창': '한식', '양갈비': '한식',
  '육회': '한식', '보쌈': '한식', '족발': '한식', '떡갈비': '한식',
  '수육': '한식', '편육': '한식', '순대': '한식', '곱창': '한식',
  '닭발': '한식', '오징어볶음': '한식', '파전': '한식',
  '메밀전병': '한식', '녹두전': '한식', '콩나물국밥': '한식', '돼지국밥': '한식',
  '꼬막': '한식', '비빔밥': '한식', '회냉면': '한식', '어죽': '한식',
  '물냉면': '한식', '불냉면': '한식', '냉면': '한식',
  '칼국수': '한식', '수제비': '한식',
  '광어': '한식', '우럭': '한식', '도다리': '한식', '낙지': '한식',
  '굴': '한식', '멍게': '한식', '해삼': '한식', '전복': '한식',
  '참돔': '한식', '모듬회': '한식', '오징어': '한식', '오징어젓갈': '한식',
  '황태': '한식', '볶음밥': '한식', '새우볶음밥': '한식', '햄볶음밥': '한식',
  '계란찜': '한식', '월남쌈': '한식', '냉채': '한식', '해물': '한식',
  '바지락': '한식', '차돌': '한식',
  '짬뽕': '중식', '탕수육': '중식', '순두부': '중식',
  '피자': '양식', '파스타': '양식', '버거': '양식', '치즈버거': '양식',
  '불고기버거': '양식', '새우버거': '양식', '카츠버거': '양식', '치킨버거': '양식',
  '샌드위치': '양식', '스테이크': '양식', '치킨': '양식',
  '감자튀김': '양식', '어니언링': '양식', '너겟': '양식', '핫도그': '양식',
  '타코': '양식', '샐러드': '양식', '스콘': '양식',
  '베이컨': '양식', '비프': '양식', '포크': '양식', '잠봉': '양식',
  '에그': '양식', '와플': '양식',
  '떡볶이': '분식', '떡말이': '분식', '강정': '분식',
  '돈까스': '일식', '우동': '일식', '소바': '일식', '가츠동': '일식',
  '라멘': '일식', '초밥': '일식', '사시미': '일식', '연어': '일식',
  '새우튀김': '일식', '가라아게': '일식', '함바그': '일식', '카레': '일식',
  '도넛': '디저트', '케이크': '디저트', '아이스크림': '디저트',
  '호두과자': '디저트', '호두떡': '디저트', '호두파이': '디저트',
  '양갱': '디저트', '단팥빵': '디저트', '바움쿠헨': '디저트', '초코': '디저트',
}

const SKIP_KEYWORDS = [
  '소주', '맥주', '사케', '막걸리', '청주', '모주',
  '사이다', '콜라', '주스', '에이드', '식혜', '라떼', '뷔페'
]

const raw    = $('메뉴 선택 입력').first().json
const body   = raw.body || raw
const userId   = body.user_id
const keywords = body.keywords || []

// 음료/뷔페 키워드면 스킵
const hasSkip = keywords.some(k => SKIP_KEYWORDS.includes(k))
if (hasSkip || keywords.length === 0) {
  return [{ json: { skipped: true, reason: 'drink or no keywords' } }]
}

// 첫 번째로 매핑되는 카테고리 사용
const category = keywords.map(k => KEYWORD_CATEGORY_MAP[k]).find(Boolean) || '기타'

const ALPHA = 0.1
const allLogits = $input.all()
const existing  = allLogits.find(i => i.json.category === category)
const currentLogit = existing ? (existing.json.logit ?? 0) : 0
const currentCount = existing ? (existing.json.sample_count ?? 0) : 0

return [{
  json: {
    user_id:      userId,
    category,
    logit:        Math.round((currentLogit + ALPHA) * 1000) / 1000,
    sample_count: currentCount + 1,
    updated_at:   new Date().toISOString()
  }
}]
```

> ⚠️ `skipped: true`가 반환되면 이후 노드(로짓 업데이트)가 실행되지 않도록  
> **IF 노드**를 앞에 추가하거나, Supabase Update 노드의 조건을 확인하세요.

### 로짓 업데이트 — Supabase Update

- Table: `user_preference_logits`
- **Condition**: `user_id = {{ $json.user_id }}`, `category = {{ $json.category }}`
- **Fields**: `logit`, `sample_count`, `updated_at`

### 선택 완료 출력 — Respond to Webhook

| 항목 | 값 |
|------|-----|
| **Respond With** | JSON |
| **Response Body** | `={{ JSON.stringify({ ok: true, category: $json.category }) }}` |

---

## ✅ Step 3 체크리스트

```
[x] n8n B: 응답 포맷 — menus 추가
[x] n8n C: 응답 포맷 — menus 추가
[x] React Restaurant interface — menus 필드 추가
[x] React handleMenuRequest — API 제거, restaurant.menus 직접 사용
[ ] n8n food/select webhook 추가 (6개 노드)
[ ] keyword→category 매핑 Code 노드 삽입
[ ] 음료/뷔페 스킵 로직 확인
[ ] 테스트: 식당 카드 클릭 → 메뉴 패널 정상 표시
[ ] 테스트: 메뉴 선택 → logit +0.1 확인
[ ] 테스트: 소주 선택 → skipped: true 확인 (logit 변화 없음)
```

완료 → [Step 4: 월드컵](n8n_04_worldcup.md) 진행
